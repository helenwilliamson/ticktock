using UnityEngine;
using System.Collections;

public class CharacterKeeperAlive : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//gameObject.transform.RotateAround(Vector3.zero, Vector3.up, 1);
		//var z = gameObject.transform.localRotation.z;
		//gameObject.transform.localRotation = Quaternion.Euler(0f, 0f, z+90);
	}
}
